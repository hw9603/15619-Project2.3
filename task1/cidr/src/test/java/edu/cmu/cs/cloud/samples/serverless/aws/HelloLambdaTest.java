package edu.cmu.cs.cloud.samples.serverless.aws;

import static org.junit.Assert.assertEquals;
import static org.mockito.Mockito.mock;

import org.junit.Test;

import com.amazonaws.services.lambda.runtime.Context;

public class HelloLambdaTest {

    @Test
    public void invokeHelloTest() {
//        HelloLambda helloLamda = new HelloLambda();

//        Context context = mock(Context.class);

//        String result = helloLamda.handleRequest(5, context);

//        assertEquals("5 squared is 25", result);
        return;
    }
}
